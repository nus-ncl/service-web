# This script demos two pc and a gateway. Communication between nodes is managed by the gateway.

set ns [new Simulator]
source tb_compat.tcl

set attacker [$ns node]

set lan0 [$ns make-lan "$attacker" 100Mb 0ms]

$ns rtproto Static

# Go!
$ns run