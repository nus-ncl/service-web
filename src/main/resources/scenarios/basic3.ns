# This is a simple experiment with 3 nodes in a LAN

set ns [new Simulator]
source tb_compat.tcl

# Add nodes
set n0 [$ns node]
set n1 [$ns node]
set n2 [$ns node]

# Make LAN
set lan0 [$ns make-lan "$n0 $n1 $n2" 10Gb 0ms]

$ns rtproto Static
$ns run