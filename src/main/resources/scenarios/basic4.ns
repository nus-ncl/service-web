# This is a simple experiment with 2 nodes and customized link specification

set ns [new Simulator]
source tb_compat.tcl

# Add nodes
set n0 [$ns node]
set n1 [$ns node]

# Add link between nodes and specify link property
set link0 [$ns duplex-link $n0 $n1 100Mb 200ms DropTail]

$ns rtproto Static
$ns run